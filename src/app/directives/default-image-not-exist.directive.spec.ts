import { DefaultImageNotExistDirective } from './default-image-not-exist.directive';

describe('DefaultImageNotExistDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultImageNotExistDirective();
    expect(directive).toBeTruthy();
  });
});
